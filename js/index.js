const inicio = document.querySelector('.nav-inicio');
const servicios = document.querySelector('.nav-servicios');
const productos = document.querySelector('.nav-productos');
const cursos = document.querySelector('.nav-cursos');
const contacto = document.querySelector('.nav-contacto');

inicio.addEventListener('click', () => {
    // logica para manejar el clic en "Inicio"
});

servicios.addEventListener('click', () => {
    // logica para manejar el clic en "Servicios"
});

productos.addEventListener('click', () => {
    // logica para manejar el clic en "Productos"
});

cursos.addEventListener('click', () => {
    // logica para manejar el clic en "Cursos"
});

contacto.addEventListener('click', () => {
    // logica para manejar el clic en "Contacto"
});